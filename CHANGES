r2: initial version
r3: gpx dumped to file not stdout
    changed arguments handling
    fixed communication with old firmware version(?)
    fixed losing bytes when block starts with \n
r4: faster download
    internal changes
r5: communication logging can be disabled (-g)
    dump binary data to one file
    POIs
    dump communication only if requested (-c)
    tracklist goes to stdout
    heading in waypoint
    verbose flag
    included kernel module source instead of patch
r6: fixed CHANGES file
r7: added heartrate to gpx
    option for using barimetric altitude instead of gps's (-a)
r8: added reading binary files (written with -b) (-f)
    fixed speed in gpx (m/s instead of km/h)
    better help
